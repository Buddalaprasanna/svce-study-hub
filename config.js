/* Change this once you deploy your backend to Render (or wherever).
   For local testing, keep it as http://localhost:5000 while running
   "npm start" inside the /server folder. */
const API_BASE_URL = "http://localhost:5000";
